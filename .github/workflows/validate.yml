name: validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  dataset:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Rebuild canonical dataset
        run: python3 tools/normalize_blocks.py
      - name: Dataset must be up to date with sources
        run: git diff --exit-code data/blocks.csv data/manifest.json
      - name: Validate dataset
        run: python3 tools/validate_blocks.py

  pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Lint shipped HTML pages (doctype, charset, script balance, dangling ids, code-in-text)
        run: python3 tools/lint_pages.py
